services:

  # Serveur web
  nginx:
    image: nginx:alpine
    container_name: quaiantique_nginx
    ports:
      - "8080:80"
    volumes:
      - ./backend:/var/www/html
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - php

  # Application Symfony
  php:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: quaiantique_php
    working_dir: /var/www/html
    volumes:
      - ./backend:/var/www/html
      - vendor_data:/var/www/html/vendor
    depends_on:
      - database
      - mongodb

  # Base de données MySQL
  database:
    image: mysql:8.0
    container_name: quaiantique_database
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: quai_antique
      MYSQL_USER: quai_antique
      MYSQL_PASSWORD: quai_antique
      MYSQL_ROOT_PASSWORD: root
    volumes:
      - database_data:/var/lib/mysql
    ports:
      - "3307:3306"

  # Base de données MongoDB
  mongodb:
    image: mongo:8
    container_name: quaiantique_mongodb
    restart: unless-stopped
    volumes:
      - mongodb_data:/data/db
    ports:
      - "27017:27017"

volumes:
  database_data:
  mongodb_data:
  vendor_data: